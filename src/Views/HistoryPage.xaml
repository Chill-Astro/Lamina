<Page
    x:Class="Lamina.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Lamina.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <!--  History of your Math not the Boring one from School  -->

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Title and Clear Button  -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="20"
                FontWeight="SemiBold"
                Text="History" />
            <Button
                Grid.Column="1"
                VerticalAlignment="Center"
                Click="ClearHistoryButton_Click"
                Content="&#xe74d;"
                FontFamily="Segoe Fluent Icons"
                Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <!--  History List  -->
        <Grid Grid.Row="1">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!--  The No History Text from FOSS Root Checker  -->
                    <TextBlock
                        x:Name="NoHistoryText"
                        Margin="10"
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Text="Aww no History? Let's Make some! :)"
                        Visibility="Collapsed" />
                    <ListView x:Name="HistoryList" MaxHeight="500" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        Margin="5"
                                        FontSize="16"
                                        Text="{Binding}"
                                        TextWrapping="Wrap" />
                                    <Button
                                        x:Name="UseButton"
                                        Grid.Column="1"
                                        Margin="5"
                                        Padding="12,6"
                                        Click="UseHistoryButton_Click"
                                        Tag="{Binding}"
                                        Content="Use"
                                        FontSize="12"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Visibility="Collapsed" />
                                    <Border
                                        Grid.ColumnSpan="2"
                                        Background="Transparent"
                                        PointerEntered="HistoryItem_PointerEntered"
                                        PointerExited="HistoryItem_PointerExited" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
