<Page
    x:Class="Lamina.Views.ApproxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Grid Background="Transparent">
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" Spacing="8">
			<TextBlock Text="Enter the Number [n] :" Style="{StaticResource BaseTextBlockStyle}"/>
			<TextBox x:Name="NumberTextBox" Width="200" HorizontalAlignment="Stretch"/>
			<Button Content="Calculate"
                    x:Name="RoundButton"
                    Click="RoundButton_Click"
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0"
                    Style="{StaticResource AccentButtonStyle}" />
            <TextBlock x:Name="RoundedTextBlock" Text="OUTPUT AREA" FontWeight="SemiBold" FontSize="12" IsTextSelectionEnabled="True" Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Center"/>
		</StackPanel>
	</Grid>        
</Page>
